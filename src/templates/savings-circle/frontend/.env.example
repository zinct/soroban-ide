# Copy this file to `.env` and fill in your deployed contract ID.
# In the Soroban IDE Deploy panel: build the counter contract, deploy it,
# then copy the contract ID that gets printed.

VITE_CONTRACT_ID=
VITE_NETWORK=TESTNET
